spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://127.0.0.1:5432/lumen}
    username: ${DATABASE_USERNAME:lumen}
    password: ${DATABASE_PASSWORD:lumen}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
